import axios from 'axios';
import https from 'https';

const url =
    'https://admin.cibpay.az/external/site/html?type=findById&id=7131194';

const formData = new URLSearchParams();

formData.append('login', 'Lisenziyalar');
formData.append('password', 'YourPasswordHere');

const httpsAgent = new https.Agent({
    rejectUnauthorized: false // like CURLOPT_SSL_VERIFYPEER => false
});

axios.post(url, formData, {
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded'
    },
    httpsAgentgit remote add origin https://github.com/tarlanchik/Lisenziyalar.git
})
    .then(response => {
        console.log(response.data);
    })
    .catch(error => {
        console.error('Error:', error.message);
    });
